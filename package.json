{
  "name": "vanilla-html",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "parcel 'src/**/*.html' --no-source-maps --port 3000",
    "build": "parcel build 'src/**/*.html' --no-source-maps",
    "preview": "npx serve dist"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.11",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "parcel": "^2.14.0",
    "posthtml-include": "^2.0.1",
    "serve": "^14.2.4"
  }
}
